CREATE TABLE tipo_usuario (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  nome_tipo VARCHAR(255)
);

CREATE TABLE item_usuario (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  nome_tipo_usuario VARCHAR(255)
);
